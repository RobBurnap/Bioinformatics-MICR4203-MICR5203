# Starter data

This directory contains small, public inputs that notebooks may download into the matching Google Drive `Data/NB##_.../` directory.

Large datasets, restricted data, student-assigned sequences, and generated results do not belong here. Each data folder includes an input contract describing the expected files.

